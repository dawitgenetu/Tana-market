// Figma-related helper removed — replaced with minimal stub to avoid
// carrying Figma-specific assets in the codebase per project scope.
export function ImageWithFallback(_props: any) {
  // simply return null in UI; callers should use native <img> or other components
  return null;
}
